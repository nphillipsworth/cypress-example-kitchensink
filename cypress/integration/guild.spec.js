/// <reference types="Cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('http://localhost:8080/commands/guild')
  })

  it('Very simple test', () => {
  
  })

  it('click the right element', () => {
  
  })

  it('Good selectors', () => {
  
  })

  it('Thinking about asynchronicity', () => {
  
  })

  it('Negative testing', () => {
  
  })

  it('Re-usable code.', () => {
  
  })

  it('Using fixtures.', () => {
  
  })
})
